#include "main.h"
#include <stdio.h>

/**
 * main - a program that prints its nae.
 * @argc: Argument counter
 * @argv: Argument Vector
 *
 * Return: Always 0.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
